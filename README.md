# generator-ushell-module
> Yeoman generator for ushell module.

## installation
```shell
cd ~/github
git clone git@github.com:afeiship/generator-ushell-module.git
cd generator-ushell-module
npm i && npm link
```
